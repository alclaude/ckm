{# src/Blogger/BlogBundle/Resources/views/Analysis/index.html.twig #}
{% form_theme form1 'CKMAppBundle::Form/errors.html.twig' %}
{% form_theme form1 'CKMAppBundle::Form/field.html.twig' %}

{% block content %}

{% for flashMessage in app.session.flashbag.get('notice') %}
  <div class="row">
    <div class="col-lg-6">
      <div class="alert alert-danger">
              <h4>Warning!</h4>
              <p>{{ flashMessage }}</p>
      </div>
    </div>
  </div>
{% endfor %}

<div class="panel panel-primary">
    <div class="panel-heading">
      <h3 class="panel-title"><i class="fa fa-arrow-circle-o-right "></i> Change reference...</h3>
    </div>
    <div class="panel-body">
      <form role="form" method="post" action="{{ path('CKMAppBundle_analyse_create_analyse_source_input_tag', {'input_id':input_id } ) }}" {{ form_enctype(form1) }}>
          <div class="form-group">
            {{ form_errors(form1.currentTag) }}
            {{ form_label(form1.currentTag) }}
            {{ form_widget(form1.currentTag) }}
          </div>
          {{ form_rest(form1) }}
          <input type="submit" class="btn btn-primary" />
        </p>
      </form>
    </div>
</div>

{% endblock %}
